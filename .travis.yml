languague: python
dist: xenial
sudo: true

python:
  - 3.8

install: 
  - pip install -r requirements.txt codecov

script:
  - pytest --cov
  - pipenv run pytest --cov=pypro
  - cp contrib/env-sample .env

after_success:
  - pipenv run codecov